<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Admin Dashboard - Bazario</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;800&display=swap" rel="stylesheet" />
  <script type="module" src="https://unpkg.com/@splinetool/viewer@1.10.7/build/spline-viewer.js"></script>
  <style>
    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      font-family: 'Inter', sans-serif;
      background: #000; /* Fully black background */
      color: #fff;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    nav {
      background: black;
      padding: 1rem 2rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
      border-bottom: 1px solid rgba(255, 255, 255, 0.08);
      box-shadow: 0 4px 12px rgba(255, 0, 0, 0.3);
      position: sticky;
      top: 0;
      z-index: 100;
    }

    .logo {
      font-size: 1.8rem;
      font-weight: 800;
      background: linear-gradient(135deg, #ff0000, #b30000);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      letter-spacing: 2px;
    }

    nav ul {
      list-style: none;
      display: flex;
      gap: 1.5rem;
      margin: 0;
    }

    nav ul li a {
      color: #fff;
      text-decoration: none;
      font-weight: 600;
      padding: 0.6rem 1rem;
      border-radius: 8px;
      transition: all 0.3s ease;
    }

    nav ul li a:hover,
    nav ul li a.active {
      background-color: #ff1a1a;
      box-shadow: 0 0 12px rgba(255, 0, 0, 0.5);
    }

    .hero {
      background: black;
      padding: 60px 20px;
      text-align: left;
      color: #fff;
      position: relative;
      overflow: hidden;
    }

    .hero::before,
    .hero::after {
      content: '';
      position: absolute;
      width: 300px;
      height: 300px;
      background: radial-gradient(circle, rgba(255,0,0,0.15), transparent 70%);
      z-index: 0;
      border-radius: 50%;
    }

    .hero::before {
      top: -50px;
      left: -100px;
    }

    .hero::after {
      bottom: -50px;
      right: -100px;
    }

    .hero h1 {
      font-size: 2.5rem;
      font-weight: 800;
      z-index: 1;
      position: relative;
      margin-bottom: 10px;
    }

    .hero p.subtext {
      font-size: 1.1rem;
      color: #ccc;
      margin-top: 10px;
      margin-bottom: 5px;
    }

    .hero p.muted {
      font-size: 0.95rem;
      color: #999;
      font-style: italic;
    }

    .hero-model {
      margin-top: 20px;
      z-index: 1;
      position: relative;
      padding: 0 40px; /* Adds black space left and right */
    }

    .spline-model {
      width: 100%;
      max-width: 700px;
      margin: 30px auto 0;
      transform: scale(1.8); /* Zoomed in more */
      transform-origin: center;
      transition: transform 0.4s ease;
    }

    spline-viewer {
      width: 100%;
      height: 400px;
      border-radius: 12px;
      overflow: hidden;
      background-color: #000; /* Forces viewer canvas to have black bg */
    }

    main {
      max-width: 1200px;
      margin: 40px auto;
      padding: 0 20px;
      flex-grow: 1;
    }

    .section-title {
      background-color: black;
      font-size: 1.8rem;
      font-weight: 700;
      color: #ff3c3c;
      margin-bottom: 20px;
      padding: 10px;
      border-radius: 8px;
    }

    footer {
      background: black;
      color: #888;
      text-align: center;
      padding: 1rem 0;
      font-size: 0.9rem;
      box-shadow: 0 -2px 8px rgba(255, 0, 0, 0.2);
    }

    @media (max-width: 768px) {
      nav {
        flex-direction: column;
        align-items: flex-start;
      }

      nav ul {
        flex-direction: column;
        gap: 10px;
        margin-top: 10px;
      }

      .hero h1 {
        font-size: 2rem;
      }

      .spline-model {
        transform: scale(1);
      }

      
    }
    .model-wrapper {
  width: 80%;
  height: 400px; /* adjust height */
  overflow: hidden;
  background-color: #0f0f0f;
  float: left;
}

.spline-zoom {
  transform: scale(1.4); /* zoom level */
  transform-origin: center;
  width: 100%;
  height: 100%;
}

.spline-zoom spline-viewer {
  width: 100%;
  height: 100%;
  border: none;
}

/* Side content taking remaining 20% */
.side-content {
  width: 20%;
  height: 400px;
  background-color: black;
  float: left;
  overflow-y: auto;
}

  </style>
</head>
<body>

  <nav>
    <div class="logo">Bazario Admin</div>
    <ul id="navLinks">
      <li><a href="admin/receivecomplain" class="active" data-section="/customer">Customers Complaints</a></li>
      <li><a href="/shopsdata" data-section="shopowners">Shops Management</a></li>
      <li><a href="/admin/logout">Logout</a></li>
    </ul>
  </nav>

  <section class="hero">
    <div>
      <h1>Welcome to Admin Dashboard</h1>
      <p class="subtext">Manage your platform with efficiency and confidence.</p>
      <p class="muted">Track shops, oversee customers and stay in control — all in one place.</p>

      <div class="model-wrapper">
  <div class="spline-zoom">
    <spline-viewer url="https://prod.spline.design/cheonekbTUFzPiGO/scene.splinecode"></spline-viewer>
  </div>
</div>
<div class="side-content">
  <!-- You can put scrollable content here -->
</div>

    </div>
  </section>

  <main>
    <div class="section-title">📊 Admin Overview</div>
    <p>Start managing your customers, shops, and more. Choose a section above to proceed.</p>
  </main>

  <footer>
    &copy; 2025 Bazario. All rights reserved.
  </footer>

</body>
</html>
