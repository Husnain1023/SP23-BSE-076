<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Rider Login | Bazario</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
      background: #0a0a0a;
      min-height: 100vh;
      overflow-x: hidden;
      position: relative;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    /* Animated background with geometric shapes */
    .bg-container {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: linear-gradient(135deg, #0a0a0a 0%, #1a1a2e 50%, #16213e 100%);
      z-index: -2;
    }

    .bg-shapes {
      position: absolute;
      width: 100%;
      height: 100%;
      overflow: hidden;
    }

    .shape {
      position: absolute;
      background: linear-gradient(45deg, #00d4ff, #0099cc);
      opacity: 0.1;
      border-radius: 50%;
      animation: float 8s ease-in-out infinite;
    }

    .shape:nth-child(1) { width: 300px; height: 300px; top: 10%; left: 10%; animation-delay: 0s; }
    .shape:nth-child(2) { width: 200px; height: 200px; top: 60%; right: 15%; animation-delay: 2s; }
    .shape:nth-child(3) { width: 150px; height: 150px; top: 80%; left: 60%; animation-delay: 4s; }
    .shape:nth-child(4) { width: 250px; height: 250px; top: 20%; right: 40%; animation-delay: 1s; }

    @keyframes float {
      0%, 100% { transform: translateY(0px) scale(1); }
      50% { transform: translateY(-30px) scale(1.1); }
    }

    /* Grid pattern overlay */
    .grid-overlay {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background-image: 
        linear-gradient(rgba(0, 212, 255, 0.03) 1px, transparent 1px),
        linear-gradient(90deg, rgba(0, 212, 255, 0.03) 1px, transparent 1px);
      background-size: 50px 50px;
      z-index: -1;
    }

    /* Header */
    .header {
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      height: 80px;
      background: rgba(10, 10, 10, 0.9);
      backdrop-filter: blur(20px);
      border-bottom: 1px solid rgba(0, 212, 255, 0.1);
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 0 60px;
      z-index: 100;
    }

    .logo-header {
      display: flex;
      align-items: center;
      gap: 15px;
    }

    .logo-icon {
      width: 50px;
      height: 50px;
      background: linear-gradient(135deg, #00d4ff, #0099cc);
      border-radius: 12px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.8rem;
      font-weight: 900;
      color: white;
      box-shadow: 0 8px 25px rgba(0, 212, 255, 0.3);
    }

    .logo-text {
      font-size: 1.5rem;
      font-weight: 700;
      color: #fff;
    }

    /* Main login container */
    .login-container {
      margin-top: 80px;
      padding: 60px;
      min-height: calc(100vh - 80px);
      display: flex;
      align-items: center;
      justify-content: center;
      width: 100%;
    }

    .login-wrapper {
      display: grid;
      grid-template-columns: 1fr 500px;
      gap: 80px;
      max-width: 1200px;
      width: 100%;
      align-items: center;
    }

    /* Welcome section */
    .welcome-section {
      padding: 40px 0;
    }

    .welcome-title {
      font-size: 3.5rem;
      font-weight: 900;
      color: #fff;
      margin-bottom: 20px;
      line-height: 1.1;
    }

    .welcome-subtitle {
      font-size: 1.2rem;
      color: #888;
      margin-bottom: 40px;
      line-height: 1.6;
    }

    .highlight {
      background: linear-gradient(135deg, #00d4ff, #0099cc);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    .features-list {
      display: flex;
      flex-direction: column;
      gap: 20px;
    }

    .feature-item {
      display: flex;
      align-items: center;
      gap: 15px;
      color: #ccc;
      font-size: 1.1rem;
    }

    .feature-icon {
      width: 45px;
      height: 45px;
      background: linear-gradient(135deg, #00d4ff, #0099cc);
      border-radius: 10px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.3rem;
      color: white;
      box-shadow: 0 5px 15px rgba(0, 212, 255, 0.2);
    }

    /* Login box */
    .login-box {
      background: rgba(255, 255, 255, 0.05);
      border: 1px solid rgba(0, 212, 255, 0.1);
      border-radius: 25px;
      padding: 50px 40px;
      backdrop-filter: blur(20px);
      box-shadow: 0 25px 50px rgba(0, 0, 0, 0.3);
      position: relative;
      color: white;
    }

    .login-box::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 3px;
      background: linear-gradient(90deg, #00d4ff, #0099cc);
      border-radius: 25px 25px 0 0;
    }

    .login-header {
      text-align: center;
      margin-bottom: 40px;
    }

    .login-icon {
      width: 80px;
      height: 80px;
      background: linear-gradient(135deg, #00d4ff, #0099cc);
      border-radius: 20px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 2.5rem;
      color: white;
      margin: 0 auto 20px;
      box-shadow: 0 15px 35px rgba(0, 212, 255, 0.3);
    }

    .login-title {
      font-size: 2.2rem;
      font-weight: 700;
      color: #fff;
      margin-bottom: 10px;
    }

    .login-subtitle {
      font-size: 1rem;
      color: #888;
      line-height: 1.5;
    }

    /* Form styles */
    .form-group {
      margin-bottom: 25px;
    }

    .form-label {
      display: block;
      margin-bottom: 8px;
      font-size: 0.95rem;
      font-weight: 500;
      color: #ccc;
    }

    .form-input {
      width: 100%;
      padding: 15px 20px;
      border: 1px solid rgba(0, 212, 255, 0.2);
      border-radius: 12px;
      background: rgba(255, 255, 255, 0.05);
      color: #fff;
      font-size: 1rem;
      transition: all 0.3s ease;
      backdrop-filter: blur(10px);
    }

    .form-input:focus {
      outline: none;
      border-color: #00d4ff;
      box-shadow: 0 0 20px rgba(0, 212, 255, 0.2);
      background: rgba(255, 255, 255, 0.08);
    }

    .form-input::placeholder {
      color: #666;
    }

    .form-options {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 30px;
      font-size: 0.9rem;
    }

    .checkbox-wrapper {
      display: flex;
      align-items: center;
      gap: 8px;
      color: #ccc;
    }

    .checkbox {
      width: 18px;
      height: 18px;
      accent-color: #00d4ff;
    }

    .forgot-link {
      color: #00d4ff;
      text-decoration: none;
      transition: color 0.3s ease;
    }

    .forgot-link:hover {
      color: #0099cc;
      text-decoration: underline;
    }

    .login-btn {
      width: 100%;
      padding: 18px;
      background: linear-gradient(135deg, #00d4ff, #0099cc);
      border: none;
      border-radius: 12px;
      font-size: 1.1rem;
      font-weight: 600;
      color: white;
      cursor: pointer;
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      box-shadow: 0 15px 35px rgba(0, 212, 255, 0.2);
      margin-bottom: 25px;
    }

    .login-btn:hover {
      transform: translateY(-3px);
      box-shadow: 0 20px 45px rgba(0, 212, 255, 0.3);
    }

    .login-btn:active {
      transform: translateY(-1px);
    }

    .register-link {
      text-align: center;
      font-size: 0.95rem;
      color: #888;
    }

    .register-link a {
      color: #00d4ff;
      text-decoration: none;
      font-weight: 500;
      transition: color 0.3s ease;
    }

    .register-link a:hover {
      color: #0099cc;
      text-decoration: underline;
    }

    /* Floating bike */
    .floating-bike {
      position: fixed;
      bottom: 30px;
      right: 30px;
      font-size: 4rem;
      color: #00d4ff;
      opacity: 0.6;
      animation: bounce 3s ease-in-out infinite;
      z-index: 10;
    }

    @keyframes bounce {
      0%, 100% { transform: translateY(0px); }
      50% { transform: translateY(-15px); }
    }
    .error-message {
  background-color: #f87171;
  color: white;
  padding: 10px 16px;
  border-radius: 8px;
  margin-bottom: 20px;
  text-align: center;
  font-weight: 500;
}
    /* Glow effects */
    .glow {
      position: relative;
    }

    .glow::before {
      content: '';
      position: absolute;
      top: -2px;
      left: -2px;
      right: -2px;
      bottom: -2px;
      background: linear-gradient(45deg, #00d4ff, #0099cc, #00d4ff);
      border-radius: inherit;
      z-index: -1;
      filter: blur(10px);
      opacity: 0;
      transition: opacity 0.3s ease;
    }

    .glow:hover::before {
      opacity: 0.7;
    }

    /* Responsive design */
    @media (max-width: 1024px) {
      .login-wrapper {
        grid-template-columns: 1fr;
        gap: 40px;
        text-align: center;
      }
      
      .welcome-title {
        font-size: 2.5rem;
      }
      
      .header {
        padding: 0 30px;
      }
      
      .login-container {
        padding: 30px;
      }
    }

    @media (max-width: 768px) {
      .login-box {
        padding: 40px 30px;
      }
      
      .welcome-title {
        font-size: 2rem;
      }
      
      .login-title {
        font-size: 1.8rem;
      }
      
      .header {
        padding: 0 20px;
      }
      
      .login-container {
        padding: 20px;
      }
    }
  </style>
</head>
<body>
  <!-- Background -->
  <div class="bg-container">
    <div class="bg-shapes">
      <div class="shape"></div>
      <div class="shape"></div>
      <div class="shape"></div>
      <div class="shape"></div>
    </div>
  </div>
  <div class="grid-overlay"></div>

  <!-- Header -->
  <header class="header">
    <div class="logo-header">
      <div class="logo-icon">BZ</div>
      <div class="logo-text">Bazario</div>
    </div>
  </header>

  <!-- Main Content -->
  <main class="login-container">
    <div class="login-wrapper">
      <!-- Welcome Section -->
      <div class="welcome-section">
        <h1 class="welcome-title">
          Welcome Back<br>
          <span class="highlight">Rider</span>
        </h1>
        <p class="welcome-subtitle">
          Access your delivery dashboard and continue earning with Bazario's 
          cutting-edge delivery platform. Your success is our priority.
        </p>

        <div class="features-list">
          <div class="feature-item">
            <div class="feature-icon">⚡</div>
            <span>Real-time order tracking and management</span>
          </div>
          <div class="feature-item">
            <div class="feature-icon">💰</div>
            <span>Instant earnings and payment insights</span>
          </div>
          <div class="feature-item">
            <div class="feature-icon">📊</div>
            <span>Detailed performance analytics</span>
          </div>
          <div class="feature-item">
            <div class="feature-icon">🎯</div>
            <span>Optimized delivery route planning</span>
          </div>
        </div>
      </div>

      <!-- Login Section -->
      <div class="login-box glow">
        <div class="login-header">
          <div class="login-icon">🚴‍♂</div>
          <h2 class="login-title">Rider Login</h2>
            <% if (typeof message !== 'undefined' && message) { %>
    <div class="error-message"><%= message %></div>
         <% } %>
          <p class="login-subtitle">Enter your credentials to access your dashboard</p>
        </div>

        <form action="/rider/login" method="POST" autocomplete="off">
          <div class="form-group">
            <label for="email" class="form-label">Email Address</label>
            <input 
              type="email" 
              id="email" 
              name="email" 
              class="form-input" 
              placeholder="Enter your email"
              required
            >
          </div>

          <div class="form-group">
            <label for="password" class="form-label">Password</label>
            <input 
              type="password" 
              id="password" 
              name="password" 
              class="form-input" 
              placeholder="Enter your password"
              required
            >
          </div>

          <div class="form-options">
            <div class="checkbox-wrapper">
              <input type="checkbox" id="remember" name="remember" class="checkbox">
              <label for="remember">Remember me</label>
            </div>
            <a href="#" class="forgot-link">Forgot Password?</a>
          </div>

          <button type="submit" class="login-btn">
            Sign In to Dashboard
          </button>
        </form>

        <div class="register-link">
          Don't have an account? <a href="/rider/register">Join as a Rider</a>
        </div>
      </div>
    </div>
  </main>

  <!-- Floating bike -->
  <div class="floating-bike">🚴‍♂</div>

  <script>
    // Add interactive effects
    document.addEventListener('DOMContentLoaded', function() {
      // Form animations
      const inputs = document.querySelectorAll('.form-input');
      inputs.forEach(input => {
        input.addEventListener('focus', function() {
          this.parentElement.style.transform = 'translateY(-2px)';
        });
        input.addEventListener('blur', function() {
          this.parentElement.style.transform = '';
        });
      });

      // Button click effect
      const loginBtn = document.querySelector('.login-btn');
      loginBtn.addEventListener('click', function(e) {
        e.preventDefault();
        
        this.style.transform = 'scale(0.98)';
        this.innerHTML = 'Signing In...';
        
        setTimeout(() => {
          this.style.transform = '';
          this.innerHTML = 'Sign In to Dashboard';
          // Here you would normally submit the form
          document.querySelector('form').submit();
        }, 1000);
      });

      // Parallax effect for shapes
      let mouseX = 0, mouseY = 0;
      document.addEventListener('mousemove', function(e) {
        mouseX = e.clientX / window.innerWidth;
        mouseY = e.clientY / window.innerHeight;
        
        const shapes = document.querySelectorAll('.shape');
        shapes.forEach((shape, index) => {
          const speed = (index + 1) * 0.02;
          const x = (mouseX - 0.5) * speed * 100;
          const y = (mouseY - 0.5) * speed * 100;
          shape.style.transform = translate(${x}px, ${y}px);
        });
      });

      // Feature icons animation
      const featureIcons = document.querySelectorAll('.feature-icon');
      featureIcons.forEach((icon, index) => {
        setTimeout(() => {
          icon.style.animation = 'bounce 2s ease-in-out infinite';
          icon.style.animationDelay = ${index * 0.2}s;
        }, 1000);
      });
    });
  </script>
</body>
</html>
