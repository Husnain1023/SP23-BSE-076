<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registration Page</title>
    <style>
        /* General Reset */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: Arial, sans-serif;
        }

        body {
            display: flex;
            height: 100vh;
            background: linear-gradient(135deg, cornsilk,rgb(92, 121, 121));
            align-items: center;
            justify-content: center;
        }

        .container {
            display: flex;
            background-color: #fff;
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.3);
            max-width: 900px;
            width: 100%;
        }

        /* Left Side */
        .left-panel {
            position: relative;
            width: 40%;
            background: url('registration1.jpg');
            background-size: cover;
            background-position: center;
        }

        .left-panel::after {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.5);
        }

        .left-panel h2 {
            position: absolute;
            bottom: 20px;
            left: 20px;
            color: #fff;
            font-size: 24px;
            z-index: 1;
        }

        /* Right Side */
        .right-panel {
            padding: 50px;
            width: 60%;
            background: linear-gradient(rgb(235, 232, 225),rgb(44, 62, 62));
        }

        .right-panel h2 {
            margin-bottom: 20px;
            color: #333;
            font-size: 32px;
            font-weight: bold;
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-group label {
            display: block;
            margin-bottom: 5px;
            color: #666;
            font-weight: bold;
        }

        .form-group input {
            width: 100%;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 5px;
        }

        .form-group input:focus {
            border-color: #00BFFF;
            outline: none;
        }

        .form-group input[type="checkbox"] {
            width: auto;
            margin-right: 10px;
        }

        .terms {
            display: flex;
            align-items: center;
            font-size: 14px;
            color: #666;
        }

        .terms a {
            color: #eef1f2;
            text-decoration: none;
        }

        .btn {
            display: block;
            width: 100%;
            background-color: #192930;
            color: #fff;
            padding: 10px;
            border: none;
            border-radius: 5px;
            font-size: 16px;
            font-weight: bold;
            cursor: pointer;
            text-align: center;
            transition: background 0.3s;
        }

        .btn:hover {
            background-color: #008CBA;
        }

        .sign-in {
            margin-top: 10px;
            text-align: center;
            color: #f6efef;
        }

        .sign-in a {
            color: #f6efef;
            text-decoration: none;
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Left Side -->
        <div class="left-panel">
            <h2>Welcome to Our Platform</h2>
        </div>

        <!-- Right Side -->
        <div class="right-panel">
            <h2>Register</h2>
            <form action="#" method="post">
                <div class="form-group">
                    <label for="name">Full Name</label>
                    <input type="text" id="name" name="name" placeholder="Enter your full name" required>
                </div>
                <div class="form-group">
                    <label for="email">Email</label>
                    <input type="email" id="email" name="email" placeholder="Enter your email" required>
                </div>
                <div class="form-group">
                    <label for="password">Password</label>
                    <input type="password" id="password" name="password" placeholder="Enter your password" required>
                </div>
                <div class="form-group terms">
                    <input type="checkbox" id="agree" required>
                    <label for="agree">I agree to all the statements in <a href="#">Terms of Service</a></label>
                </div>
                <button type="submit" class="btn">Sign Up</button>
                <p class="sign-in">Already have an account? <a href="/login">Sign In</a></p>
            </form>
        </div>
    </div>
</body>
</html>
