<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Buy & Shop</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet" />
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@600&family=Inter:wght@400;600&display=swap" rel="stylesheet" />
  <style>
    body {
      font-family: 'Inter', sans-serif;
      background: #111;
      color: #f5f5f5;
    }

    .navbar {
      background-color: #000;
      padding: 1.2rem 2rem;
      box-shadow: 0 2px 8px rgba(255, 0, 0, 0.2);
    }

    .navbar-brand {
      font-family: 'Playfair Display', serif;
      font-size: 1.8rem;
      font-weight: 600;
      color: #ff4d4d !important;
    }

    .nav-link {
      color: #fff !important;
      margin-right: 20px;
      font-weight: 500;
      transition: color 0.3s ease;
    }

    .nav-link:hover {
      color: #ff4d4d !important;
    }

    .hero-section {
      background: linear-gradient(to right, #1c1c1c, #000);
      min-height: 90vh;
      display: flex;
      align-items: center;
      padding: 50px 0;
    }

    .hero-section h1 {
      font-family: 'Playfair Display', serif;
      color: #fff;
    }

    .hero-section p {
      color: #bbb;
      font-size: 17px;
      line-height: 1.6;
    }

    .hero-section .btn {
      background-color: #ff4d4d;
      border: none;
      color: #fff;
      font-weight: bold;
    }

    .hero-section .btn:hover {
      background-color: #e43c3c;
    }

    .section-title {
      text-align: center;
      font-size: 30px;
      font-weight: 700;
      margin: 70px 0 30px;
      color: #ff4d4d;
      font-family: 'Playfair Display', serif;
    }

    .news-section {
      display: flex;
      gap: 30px;
      justify-content: center;
      padding: 0 40px;
      flex-wrap: wrap;
    }

    .news-card {
      background: #1f1f1f;
      border: 1px solid #333;
      padding: 25px;
      width: 300px;
      border-radius: 14px;
      box-shadow: 0 4px 12px rgba(255, 0, 0, 0.08);
      transition: transform 0.3s, box-shadow 0.3s;
    }

    .news-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 10px 20px rgba(255, 0, 0, 0.2);
    }

    .news-card img {
      width: 100%;
      height: 180px;
      object-fit: cover;
      border-radius: 12px;
      margin-bottom: 15px;
    }

    .news-card h5 {
      font-size: 17px;
      font-weight: 600;
      color: #fff;
    }

    .news-card p {
      font-size: 14px;
      color: #ccc;
      margin-bottom: 10px;
    }

    .news-card a {
      color: #ff4d4d;
      text-decoration: none;
      font-weight: 600;
      font-size: 14px;
    }

    .products-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 30px;
      padding: 0 40px 80px;
    }

    .product-card {
      background: #1a1a1a;
      border: 1px solid #333;
      border-radius: 20px;
      padding: 20px;
      text-align: center;
      transition: all 0.3s ease;
      box-shadow: 0 4px 12px rgba(255, 0, 0, 0.1);
      cursor: pointer;
    }

    .product-card:hover {
      transform: translateY(-10px) scale(1.03);
      box-shadow: 0 12px 24px rgba(255, 0, 0, 0.3);
    }

    .product-card img {
      border-radius: 10px;
      height: 200px;
      object-fit: contain;
      width: 100%;
      margin-bottom: 15px;
      background: #fff;
    }

    .product-card h6 {
      font-weight: 700;
      font-size: 16px;
      margin-bottom: 8px;
      color: #fff;
    }

    .product-card p {
      font-size: 14px;
      color: #aaa;
      margin-bottom: 10px;
      min-height: 40px;
    }

    .product-price {
      font-weight: bold;
      font-size: 18px;
      color: #ff4d4d;
      margin-bottom: 12px;
    }

    .availability {
      font-size: 14px;
      font-weight: 600;
      margin-bottom: 12px;
      padding: 6px 12px;
      border-radius: 20px;
      display: inline-block;
    }

    .available {
      background-color: #292;
      color: #d6f5d6;
      border: 1px solid #3c3;
    }

    .not-available {
      background-color: #662;
      color: #ffd6d6;
      border: 1px solid #d33;
    }

    .btn-shop {
      background-color: #ff4d4d;
      color: white;
      padding: 10px 18px;
      font-size: 15px;
      font-weight: 600;
      border-radius: 25px;
      text-decoration: none;
      transition: background 0.3s ease;
      display: inline-block;
    }

    .btn-shop:hover {
      background-color: #e43c3c;
    }

    /* Footer Styling */
    footer {
      background-color: #000;
      color: #ccc;
      padding: 40px 20px;
      text-align: center;
      margin-top: 60px;
      border-top: 1px solid #222;
    }

    footer .footer-logo {
      font-family: 'Playfair Display', serif;
      font-size: 28px;
      font-weight: 700;
      color: #ff4d4d;
      margin-bottom: 15px;
    }

    footer a {
      color: #ccc;
      margin: 0 10px;
      font-size: 14px;
      text-decoration: none;
      transition: color 0.3s ease;
    }

    footer a:hover {
      color: #ff4d4d;
    }

    footer p {
      margin-top: 10px;
      font-size: 13px;
      color: #888;
    }
  </style>
</head>
<body>

  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg">
    <a class="navbar-brand" href="#">BAZARIO</a>
    <div class="collapse navbar-collapse justify-content-end">
      <ul class="navbar-nav">
        <li class="nav-item"><a class="nav-link" href="/">Home</a></li>
        
       
        <li class="nav-item"><a class="nav-link" href="/totalshops">Store</a></li>
        <li class="nav-item"><a class="nav-link" href="/cart">Cart</a></li>
      </ul>
    </div>
  </nav>

  <!-- Hero Section -->
  <section class="hero-section">
    <div class="container-fluid py-5">
      <div class="row align-items-center">
        <div class="col-md-6 px-5">
          <h1 class="display-5 fw-bold mb-4">MOBILES</h1>
          <p class="lead mb-4">
            The special vase is a variant of the Japanese word for flower arrangement,
            and is designed to honor and showcase the stems and blooms.
          </p>
          <a href="#" class="btn btn-lg rounded-pill px-4 py-2">See Collection</a>
        </div>
        <div class="col-md-6 text-center">
          <img src="/image5.jpg" class="img-fluid" style="max-height: 420px; border-radius: 16px;" alt="Hero Image">
        </div>
      </div>
    </div>
  </section>

  <!-- News Section -->
  <h2 class="section-title">Our Latest Products</h2>
  <div class="news-section">
    <div class="news-card">
      <img src="/iphone.webp" alt="News 1">
      <h5>The Original is Back!</h5>
      <p>70 years since it was introduced, the iconic Egg chair returns in a refined edition.</p>
      <a href="#">See Collection</a>
    </div>
    <div class="news-card">
      <img src="/pixel.webp" alt="News 2">
      <h5>Rutjek Sale 25% Off!</h5>
      <p>Shop now and enjoy savings on our best-selling minimalist decor sets.</p>
      <a href="#">View All</a>
    </div>
    <div class="news-card">
      <img src="/image2.jpg" alt="News 3">
      <h5>Design Deals</h5>
      <p>Discover timeless Scandinavian aesthetics at special prices.</p>
      <a href="#">Explore</a>
    </div>
  </div>

  <!-- Products Section -->
  <h2 class="section-title">New Products</h2>
  <div class="products-grid">
    <% for (let i = 0; i < products.length; i++) { %>
      <div class="product-card">
        <img src="/<%= products[i].picture %>" alt="<%= products[i].title %>" />
        <h6><%= products[i].title %></h6>
        <p><%= products[i].description %></p>
        <div class="product-price">PKR <%= products[i].price %></div>
        <% if (products[i].stock >= 1) { %>
          <div class="availability available">Available (Stock: <%= products[i].stock %>)</div>
          <a href="/add-to-cart/<%= products[i]._id %>" class="btn-shop">Add To Cart</a>
        <% } else { %>
          <div class="availability not-available">Not Available</div>
        <% } %>
      </div>
    <% } %>
  </div>

  <!-- Footer -->
  <footer>
    <div class="footer-logo">BAZARIO</div>
    <div>
      <a href="#">Home</a> |
      <a href="#">About Us</a> |
      <a href="#">Contact</a> |
      <a href="#">Privacy Policy</a>
    </div>
    <p>&copy; 2025 Buy & Shop. All rights reserved.</p>
  </footer>

</body>
</html>
