<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Bazario | Sign Up</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700;800&display=swap" rel="stylesheet" />
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
  font-family: 'Inter', sans-serif;
  background: #000;
  min-height: 100vh;       /* ✅ Allow height to grow */
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;  /* ✅ Helps vertical stacking */
  overflow-y: auto;        /* ✅ Enables vertical scrolling */
  position: relative;
}


    .background {
      position: fixed;
      top: 0;
      left: 0;
      width: 100vw;
      height: 100vh;
      background: radial-gradient(circle at 20% 30%, rgba(255, 0, 0, 0.12) 0%, transparent 60%),
                  radial-gradient(circle at 80% 70%, rgba(255, 20, 20, 0.1) 0%, transparent 60%),
                  radial-gradient(circle at 50% 90%, rgba(255, 0, 0, 0.06) 0%, transparent 50%);
      animation: backgroundShift 20s ease-in-out infinite;
      z-index: -2;
    }

    @keyframes backgroundShift {
      0%, 100% { opacity: 1; transform: scale(1); }
      50% { opacity: 0.95; transform: scale(1.05); }
    }

    .signup-wrapper {
      background: #000; /* Solid black form background */
      border: 1px solid rgba(255, 255, 255, 0.06);
      border-radius: 16px;
      padding: 50px;
      width: 100%;
      max-width: 850px;
      animation: fadeIn 1s ease;
      box-shadow: 0 10px 60px rgba(255, 0, 0, 0.2);
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .logo {
      font-size: 3rem;
      font-weight: 800;
      background: linear-gradient(135deg, #ff0000, #ff4d4d);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      text-align: center;
      margin-bottom: 20px;
    }

    .form-header {
      text-align: center;
      margin-bottom: 40px;
    }

    .form-header h2 {
      color: #ffffff;
      font-size: 1.8rem;
      font-weight: 600;
      margin-bottom: 8px;
    }

    .form-header p {
      color: #aaaaaa;
      font-size: 0.95rem;
    }

    form {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 24px;
    }

    .form-group {
      display: flex;
      flex-direction: column;
    }

    .form-label {
      color: #cbd5e1;
      font-size: 0.85rem;
      margin-bottom: 6px;
    }

    .form-input {
      padding: 14px 16px;
      border: 1px solid rgba(255, 255, 255, 0.08);
      border-radius: 10px;
      background: rgba(255, 255, 255, 0.02);
      color: #fff;
      font-size: 0.95rem;
      transition: 0.3s ease;
      outline: none;
    }

    .form-input::placeholder {
      color: #64748b;
    }

    .form-input:focus {
      border-color: #ff4d4d;
      background-color: rgba(255, 255, 255, 0.05);
      box-shadow: 0 0 0 3px rgba(255, 0, 0, 0.12);
    }

    .form-actions {
      grid-column: span 2;
      margin-top: 20px;
    }

    .signup-btn {
      width: 100%;
      padding: 14px;
      font-weight: 600;
      font-size: 1rem;
      border: none;
      border-radius: 10px;
      background: linear-gradient(135deg, #ff0000, #ff4d4d);
      color: #fff;
      cursor: pointer;
      transition: 0.3s ease;
    }

    .signup-btn:hover {
      transform: translateY(-1px);
      box-shadow: 0 15px 30px rgba(255, 0, 0, 0.3);
    }

    .signup-btn:active {
      transform: translateY(0);
    }

    .login-section {
      text-align: center;
      margin-top: 24px;
    }

    .login-section p {
      color: #aaaaaa;
      font-size: 0.9rem;
    }

    .login-section a {
      color: #ff4d4d;
      text-decoration: none;
      font-weight: 600;
      margin-left: 6px;
      transition: 0.2s ease;
    }

    .login-section a:hover {
      text-decoration: underline;
    }

    @media (max-width: 768px) {
      .signup-wrapper {
        padding: 30px;
      }

      form {
        grid-template-columns: 1fr;
      }

      .form-actions {
        grid-column: span 1;
      }

      .logo {
        font-size: 2.5rem;
      }
    }
  </style>
</head>
<body>
  <div class="background"></div>

  <div class="signup-wrapper">
    <div class="logo">BAZARIO</div>
    <div class="form-header">
      <h2>Create Your Account</h2>
      <p>Join Bazario and start shopping premium</p>
    </div>

    <form method="POST" action="/customersignup">
      <div class="form-group">
        <label class="form-label">Full Name</label>
        <input name="name" class="form-input" placeholder="John Doe" required />
      </div>
      <div class="form-group">
        <label class="form-label">Email</label>
        <input type="email" name="email" class="form-input" placeholder="you@example.com" required />
      </div>
      <div class="form-group">
        <label class="form-label">Phone</label>
        <input name="phone" class="form-input" placeholder="+92xxxxxxxxx" required />
      </div>
      <div class="form-group">
        <label class="form-label">Address</label>
        <input name="address" class="form-input" placeholder="Street, City, Country" required />
      </div>
      <div class="form-group">
        <label class="form-label">Password</label>
        <input type="password" name="password" class="form-input" placeholder="••••••••" required />
      </div>
      <div class="form-group"></div>

      <div class="form-actions">
        <button type="submit" class="signup-btn">Sign Up</button>
      </div>
    </form>

    <div class="login-section">
      <p>Already have an account?
        <a href="/customerlogin">Log In</a>
      </p>
    </div>
  </div>
</body>
</html>
