<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Verify Email - Bazario</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet" />
  <style>
    body {
      background: radial-gradient(circle at top left, #1a0000, #0f0f0f);
      font-family: 'Inter', sans-serif;
      margin: 0;
      padding: 0;
      color: #fff;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
      overflow: hidden;
    }

    h2 {
      font-size: 2.8rem;
      margin-bottom: 0.4rem;
      background: linear-gradient(90deg, #ff3c3c, #ff1a1a);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      text-align: center;
      letter-spacing: 1px;
    }

    p {
      font-size: 1.05rem;
      margin-bottom: 2rem;
      color: #bbb;
      text-align: center;
    }

    form {
      background: rgba(255, 255, 255, 0.06);
      padding: 2.5rem 2rem;
      border-radius: 18px;
      box-shadow: 0 0 35px rgba(255, 0, 0, 0.3);
      backdrop-filter: blur(18px);
      display: flex;
      flex-direction: column;
      width: 90%;
      max-width: 440px;
      border: 1px solid rgba(255, 255, 255, 0.1);
    }

    input[type="text"] {
      padding: 1rem;
      border-radius: 10px;
      border: 1px solid rgba(255, 255, 255, 0.08);
      font-size: 1rem;
      margin-bottom: 2rem;
      outline: none;
      background-color: #1f1f1f;
      color: #fff;
      box-shadow: inset 0 0 12px rgba(255, 0, 0, 0.15);
      transition: border 0.3s ease;
    }

    input[type="text"]:focus {
      border-color: #ff3c3c;
    }

    input[type="text"]::placeholder {
      color: #aaa;
    }

    button {
      padding: 1rem;
      border-radius: 10px;
      border: none;
      background: linear-gradient(90deg, #ff1a1a, #b30000);
      color: #fff;
      font-weight: 700;
      font-size: 1.05rem;
      cursor: pointer;
      transition: all 0.3s ease;
      box-shadow: 0 0 18px rgba(255, 0, 0, 0.3);
    }

    button:hover {
      transform: translateY(-2px);
      background: linear-gradient(90deg, #ff4d4d, #cc0000);
      box-shadow: 0 0 28px rgba(255, 0, 0, 0.5);
    }

    @media (max-width: 480px) {
      h2 {
        font-size: 2.1rem;
      }
      form {
        padding: 2rem 1.5rem;
      }
    }
  </style>
</head>
<body>
  <h2>Verify Your Email</h2>
  <p>We've sent a one-time password (OTP) to your email. Please enter it below to continue:</p>
  <form method="POST" action="/verify-otp">
    <input type="text" name="otp" placeholder="Enter 6-digit OTP" required />
    <button type="submit">Verify & Complete Signup</button>
  </form>
</body>
</html>